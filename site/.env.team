# Loaded by `vite build --mode team` (see package.json -> build:team).
# Drives SECTIONS in src/config/profile.ts, which Rollup then uses to drop the
# restricted sections from the bundle.
VITE_PROFILE=team
